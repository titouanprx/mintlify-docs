---
title: 'List Sent Invitations'
api: 'POST /v2/network'
description: 'Retrieve a paginated list of sent invitations.'
---

## Overview

Returns a paginated list of connection invitations sent from the account. Pagination is **offset-based** — set `count` for the page size and `offset` to skip ahead.

<Note>This endpoint consumes **1 credit per 50 results** returned. LinkedIn returns up to **100 invitations per internal request**, so larger `count` values trigger multiple upstream calls automatically.</Note>

## Request

<ParamField body="account_id" type="string" required>
  The unique identifier of the account to use.
</ParamField>

<ParamField body="action" type="string" required>
  Must be `"list_sent"`.
</ParamField>

<ParamField body="params" type="object">
  <Expandable title="params properties">
    <ParamField body="count" type="integer" default={10}>
      Number of sent invitations to return. Defaults to `10`. **Capped at `1000`** per call — to retrieve more, paginate with `offset`.
    </ParamField>
    <ParamField body="offset" type="integer" default={0}>
      Zero-indexed offset into the sent invitations list. Defaults to `0`. Use `pagination.next_offset` from a previous response to fetch the next page.
    </ParamField>
    <ParamField body="invitation_type" type="string" default="CONNECTION">
      Filter by invitation type. One of: `"CONNECTION"`, `"ORGANIZATION"`, `"EVENT"`. Defaults to `"CONNECTION"`.
    </ParamField>
  </Expandable>
</ParamField>

<RequestExample>
```json First page
{
  "account_id": "your-account-id",
  "action": "list_sent",
  "params": {
    "count": 50,
    "offset": 0
  }
}
```

```json Next page
{
  "account_id": "your-account-id",
  "action": "list_sent",
  "params": {
    "count": 50,
    "offset": 50,
    "invitation_type": "CONNECTION"
  }
}
```
</RequestExample>

## Response

<ResponseField name="success" type="boolean">
  Indicates whether the request was successful.
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="data properties">
    <ResponseField name="invitations" type="array">
      List of sent invitation objects with recipient info, invitation type, and timing.
    </ResponseField>
    <ResponseField name="total_results" type="integer">
      Number of sent invitations returned in this response.
    </ResponseField>
    <ResponseField name="total_available_results" type="integer">
      Total number of sent invitations available on the account.
    </ResponseField>
    <ResponseField name="invitation_type" type="string">
      The invitation type filter that was applied.
    </ResponseField>
    <ResponseField name="pagination" type="object">
      <Expandable title="pagination properties">
        <ResponseField name="offset" type="integer">Offset of the first invitation in this response.</ResponseField>
        <ResponseField name="count" type="integer">Number of sent invitations returned (mirrors `total_results`).</ResponseField>
        <ResponseField name="next_offset" type="integer">Offset to send as `offset` in the next request. `null` when there are no more results.</ResponseField>
        <ResponseField name="has_more" type="boolean">`true` if more sent invitations are available beyond this batch.</ResponseField>
        <ResponseField name="results_per_page" type="integer">Maximum batch size used per upstream request.</ResponseField>
        <ResponseField name="pages_fetched" type="integer">Number of internal upstream batches consumed to assemble this response.</ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metadata" type="object">
  <Expandable title="metadata properties">
    <ResponseField name="action" type="string">The action that was performed (`"list_sent"`).</ResponseField>
    <ResponseField name="account_id" type="string">The account ID used for the request.</ResponseField>
    <ResponseField name="credits_consumed" type="integer">Number of credits consumed (1 per 50 results).</ResponseField>
    <ResponseField name="timestamp" type="string">ISO 8601 timestamp of the response.</ResponseField>
  </Expandable>
</ResponseField>

<ResponseExample>
```json 200
{
  "success": true,
  "data": {
    "invitations": [
      {
        "entity_urn": "urn:li:invitation:987654321",
        "recipient": {
          "first_name": "Bob",
          "last_name": "Williams",
          "headline": "CTO at StartupXYZ",
          "linkedin_url": "https://www.linkedin.com/in/bobwilliams",
          "profile_picture": "https://media.licdn.com/dms/image/..."
        },
        "invitation_type": "CONNECTION",
        "sent_time": "5d",
        "sent_time_days": 5
      }
    ],
    "total_results": 1,
    "total_available_results": 145,
    "invitation_type": "CONNECTION",
    "pagination": {
      "offset": 0,
      "count": 1,
      "next_offset": 1,
      "has_more": true,
      "results_per_page": 10,
      "pages_fetched": 1
    }
  },
  "metadata": {
    "action": "list_sent",
    "account_id": "your-account-id",
    "credits_consumed": 1,
    "timestamp": "2026-04-26T10:35:00Z"
  }
}
```

```json 400
{
  "success": false,
  "error": {
    "code": "INVALID_REQUEST",
    "message": "account_id is required."
  },
  "metadata": {
    "action": "list_sent",
    "account_id": null,
    "credits_consumed": 0,
    "timestamp": "2026-04-26T10:35:00Z"
  }
}
```
</ResponseExample>
