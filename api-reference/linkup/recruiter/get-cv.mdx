---
title: "Get CV"
api: "POST /v1/recruiter/cv"
description: "Retrieve a candidate's CV from a LinkedIn job application"
---

Retrieve the CV/resume and contact information for a specific candidate who has applied to your LinkedIn job posting.

<Note>
  This endpoint costs **1 credit** per request.
</Note>

### Header Parameters

<ParamField header="x-api-key" type="string" required>
  Your API key
</ParamField>

### Body Parameters

<ParamField body="application_id" type="string" required>
  LinkedIn application ID of the candidate
</ParamField>

<ParamField body="login_token" type="string" required>
  LinkedIn authentication cookie (li_at) obtained from the login/verify process
</ParamField>

<ParamField body="country" type="string" default="FR">
  Country code for proxy selection. Available: (US,UK,FR)
</ParamField>

### Response

<ResponseField name="status" type="string">
  Request status (success/error)
</ResponseField>

<ResponseField name="data" type="object">
  Candidate data
</ResponseField>

<ResponseField name="data.name" type="string">
  Full name of the candidate
</ResponseField>

<ResponseField name="data.headline" type="string">
  Professional headline of the candidate
</ResponseField>

<ResponseField name="data.location" type="string">
  Location of the candidate
</ResponseField>

<ResponseField name="data.email" type="string">
  Email address of the candidate
</ResponseField>

<ResponseField name="data.phone" type="string">
  Phone number of the candidate
</ResponseField>

<ResponseField name="data.resume_url" type="string">
  Direct URL to download the candidate's CV/resume
</ResponseField>

<ResponseExample>
```json Success Response
{
  "status": "success",
  "data": {
    "name": "Pierre Dubois",
    "headline": "Marketing & Communication. BtoB, BtoC. \nGastronomie et Hôtellerie expert.",
    "location": "Lyon, Auvergne-Rhône-Alpes, France",
    "email": "pierre.dubois@example.com",
    "phone": "+33012345678",
    "resume_url": "https://www.linkedin.com/resume/pierre-dubois"
  }
}
```
</ResponseExample>